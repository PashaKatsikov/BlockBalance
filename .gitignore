*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
build_preview/
captures/
.externalNativeBuild/
.cxx/
keystore.properties
keystore/keystore.properties
*.jks
*.keystore

# Gray part — per-project seed + credentials, never committed.
gray.properties

# Per-project uniqueness manifest — do not commit.
.uniqueness/
