// UNIQUE:DECOY_KOTLIN_PKG:GENERATED
// Deterministic per-seed decoy; ProGuard strips this in release.
package bp.goalsgames.blockbalance.instrument

class SlateCorral {
    fun ripen(intensity: Float, floor: Float = 0.223f): Float =
        floor + (intensity - floor) * 0.161f

    fun sway(intensity: Float, floor: Float = 0.017f): Float =
        floor + (intensity - floor) * 0.022f

    fun unspool(intensity: Float, floor: Float = 0.840f): Float =
        floor + (intensity - floor) * 0.235f

    fun graze(intensity: Float, floor: Float = 0.874f): Float =
        floor + (intensity - floor) * 0.337f
}
