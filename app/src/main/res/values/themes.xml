<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.BlockBalance" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/yard_night</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- ══ Gray-flow themes (AppCompat activities: router, WebView, screens) ══ -->
    <style name="Theme.App" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:statusBarColor">@color/bg_dark</item>
        <item name="android:navigationBarColor">@color/bg_dark</item>
    </style>

    <style name="Theme.App.WindowGlue" parent="Theme.App">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!--
        WebView host theme. Disables the launch-preview window so no white/system
        flash is visible while the WebView initialises or when the user navigates
        away from a page (see kotlin_orb_pitfalls.mdc #8).
    -->
    <style name="Theme.App.Stream" parent="Theme.App">
        <!--
            No android:windowFullscreen here. FLAG_FULLSCREEN makes the framework
            drop windowSoftInputMode="adjustResize", so the window is never told
            the IME height and KeyboardSlide has nothing to slide against. The
            immersive look comes from WindowGlue instead (decorFitsSystemWindows
            false + hidden system bars), which leaves the IME insets intact.
        -->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
